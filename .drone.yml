kind: pipeline
name: drone

workspace:
  base: /opt
  path: src/first-project

steps:
  - name: build
    image: golang:1.12
    environment:
      - GOPATH=/go:/opt/src/first-project
    commands:
      - go build -o game-server src/main.go
      - ./game-server
