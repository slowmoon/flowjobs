kind: pipeline
name: drone

workspace:
  base: /opt
  path: src/first_project

steps:
  - name: build
    image: golang:1.12
    environment:
      GOPATH: /go:/opt/src/first_project
    commands:
      - export $GOPATH
      - go build 
      - ./game-server
