kind: pipeline
name: drone

workspace:
  base: /opt
  path: src/first-project

steps:
  - name: build
    image: golang:1.12
    environment:
      PATH=/go:/opt/src/first-project
    commands:
      - export $PATH
      - go build 
      - ./game-server
